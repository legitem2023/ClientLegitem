import { useMutation } from '@apollo/client';
import { INSERT_VIEWS_COUNT } from 'graphql/mutation';

const useInsertView = () => {



  // return { insert_views_count };
};

export default useInsertView;
